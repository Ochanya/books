<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <link rel="stylesheet" href="styles.css">
    <title>Olivia's Books Directory</title>
  </head>
  <body>
    <h1>Olivia's Books Directory</h1>
    <div class="container">
      <div>

        <div class="user">
            <img class="profile-img" src="{{bookImgUrl}}" alt="bookcover">
            <div class="">
              <h2>{{title}}</h2>
              <div class="">{{author}}</div>

              <span>ISBN: {{isbn}} | {{pages}}pages </span>
              <span>Genre:{{genre}} | Format:{{format}}</span>
              <p>{{synopsis}}</p>

          </div>
        </div>
        <div class="user">
        </div>
      </div>

      <!-- -->
    </div>
  </body>
</html>
